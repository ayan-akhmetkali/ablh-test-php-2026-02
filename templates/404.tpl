<!doctype html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <title>{$title|escape}</title>
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>
<body>
<div class="container">
    <section class="card">
        <h1>{$title|escape}</h1>
        <p>{$message|escape}</p>
        <a href="/">На главную</a>
    </section>
</div>
</body>
</html>
